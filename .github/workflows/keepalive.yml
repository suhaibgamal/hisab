name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 9 * * 1"  # Every Monday at 9 AM UTC
  workflow_dispatch:  # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase via Hisab group page
        run: |
          curl -I -s https://hisab.suhaeb.com/group/423a5321-12a6-471c-9786-333bd9be9a30 || true
